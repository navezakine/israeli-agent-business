# n8n Workflows

Export your n8n workflows here as JSON for version control.
Build them in n8n Cloud, then **Download** each workflow's JSON and save it under this folder:

- `01-whatsapp-inbound.json`   — Build step 4 (WhatsApp inbound handler)
- `02-appointment-reminders.json` — Build step 5 (cron 08:30 reminders)
- `03-lead-followup.json`      — Build step 6 (24h lead follow-up)
- `04-hitl-approval.json`      — Build step 7 (your approve/override/cancel replies)

These files are placeholders for now — nothing to build until Step 4.
